import { Engine, EngineOptions } from "./Engine";
export declare class VueEngine extends Engine {
    #private;
    get pronto(): any;
    get vueify(): any;
    get vueServerRenderer(): any;
    $onInit(): Promise<void>;
    protected $cacheOptions(template: string, options: EngineOptions, fromFile?: boolean): EngineOptions;
    protected $compile(template: string, options: EngineOptions): (options: EngineOptions) => Promise<any>;
    protected $compileFile(file: string, options: EngineOptions): Promise<(options: EngineOptions) => any>;
    private getComponent;
    private renderComponent;
}
